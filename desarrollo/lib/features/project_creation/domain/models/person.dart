/// Persona de la universidad que puede aparecer como candidata a co-líder
/// de un proyecto.
class Person {
  const Person({required this.id, required this.name});

  final String id;
  final String name;
}
